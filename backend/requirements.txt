# Core FastAPI Stack (minimal)
fastapi==0.104.1
uvicorn[standard]==0.24.0
gunicorn==21.2.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# HTTP & Data Processing (optimized)
httpx==0.25.2  # Async HTTP client
orjson==3.9.10  # Fast JSON serialization
pandas[pyarrow]==2.1.4  # Use pyarrow backend for better performance
numpy==1.26.2

# Database (minimal)
sqlalchemy[asyncio]==2.0.23  # Enable async support
alembic==1.13.0
asyncpg==0.29.0  # Async PostgreSQL driver
aiosqlite==0.19.0  # Async SQLite driver

# Authentication (minimal)
pyjwt==2.8.0
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.3.0

# Excel & Google Sheets Export
xlsxwriter==3.1.9  # Faster than openpyxl
google-auth==2.23.0
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.1.1
google-api-python-client==2.108.0

# PDF Export (pick one)
weasyprint==60.1  # Primary PDF generator

# Testing (dev only)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0  # For mocking Google API calls
pytest-benchmark==4.0.0  # For performance testing
pytest-timeout==2.2.0  # For timeout testing
pytest-xdist==3.5.0  # For parallel testing

# Development Tools (dev only)
black==23.11.0
ruff==0.1.6
mypy==1.7.1

# Environment
python-dotenv==1.0.0