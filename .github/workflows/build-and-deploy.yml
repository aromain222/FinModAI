name: Build & Deploy to Fly.io

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build Docker Image & Push to GHCR
    runs-on: ubuntu-latest
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
      
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Lowercase repository name
        id: lowercase
        run: echo "repo_lc=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.lowercase.outputs.repo_lc }}
          tags: |
            type=ref,event=branch
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ steps.lowercase.outputs.repo_lc }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ steps.lowercase.outputs.repo_lc }}:buildcache,mode=max
          
      - name: Verify image was pushed
        run: |
          echo "✓ Image pushed successfully"
          echo "Tags: ${{ steps.meta.outputs.tags }}"
          # Pull to verify it exists
          docker pull $(echo "${{ steps.meta.outputs.tags }}" | head -1)

  deploy-to-fly:
    name: Deploy to Fly.io
    runs-on: ubuntu-latest
    needs: build-and-push
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      - name: Deploy to Fly.io
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
        run: |
          # Use the immutable SHA tag to avoid manifest unknown errors
          REPO_LC=$(echo "${{ github.repository }}" | tr '[:upper:]' '[:lower:]')
          IMAGE="${{ env.REGISTRY }}/${REPO_LC}:main-${{ github.sha }}"
          
          echo "Deploying image: $IMAGE"
          
          flyctl deploy \
            --app finmodai-z9qvtg \
            --image "$IMAGE" \
            --yes \
            --strategy immediate
          
          echo "✓ Deployment complete!"
          echo "Visit: https://finmodai.fly.dev/"

      - name: Health check
        run: |
          echo "Waiting for deployment to settle..."
          sleep 10
          
          HEALTH_URL="https://finmodai-z9qvtg.fly.dev/healthz"
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" "$HEALTH_URL")
          
          if [ "$RESPONSE" = "200" ]; then
            echo "✓ Health check passed"
            curl -s "$HEALTH_URL" | jq .
          else
            echo "✗ Health check failed with status $RESPONSE"
            exit 1
          fi

