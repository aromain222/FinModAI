{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 financial_models_ui:app",
    "healthcheckPath": "/",
    "healthcheckTimeout": 100
  }
}

